{
  "name": "@gravity-runner/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "lint": "tsc --noEmit --pretty false",
    "typecheck": "tsc --noEmit --pretty false",
    "test": "vitest run --passWithNoTests",
    "build": "tsc --pretty false && vite build",
    "preview": "vite preview"
  }
}
